#include "main.h"
#include <stdio.h>

/**
 * _strcpy - copies string to buffer
 * @dest: destination buffer
 * @src: string to be copied, ponter to
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
	dest = src;
	return (dest);
}
